# Mercedes-Benz → OHC Relay Configuration
# Copy to .env and fill in your values
# NEVER commit .env — only .env.example goes in the repo

# From https://developer.mercedes-benz.com/ → Your Project → Credentials
MB_CLIENT_ID=your-client-id-here
MB_CLIENT_SECRET=your-client-secret-here

# OAuth2 redirect — leave as-is for local auth flow
MB_REDIRECT_URI=http://localhost:9090/callback

# Your vehicle ID (VIN). Leave blank to auto-discover on first run.
MB_VEHICLE_ID=

# Where to send CloudEvents
NORTH_URL=https://north-qr-demo-qa.apps.cluster-nlthm.nlthm.sandbox3528.opentlc.com

# Poll interval in seconds (30 = gentle, 10 = aggressive, 60 = lazy)
MB_POLL_INTERVAL=30
